apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: los-frontend
spec:
  to:
    kind: Service
    name: los-frontend
  port:
    targetPort: http
  tls:
    termination: edge

# Made with Bob
